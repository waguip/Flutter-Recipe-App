import 'package:flutter/material.dart';

class CategoryModel {
  String name;
  String iconPath;
  Icon icon;
  Color boxColor;

  CategoryModel({
    required this.name,
    required this.iconPath,
    required this.icon,
    required this.boxColor,
  });
}
